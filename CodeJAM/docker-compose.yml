version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: codejam-postgres
    environment:
      POSTGRES_USER: codejam
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-codejam123}
      POSTGRES_DB: codejam
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U codejam"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Redis (Job Queue)
  redis:
    image: redis:7-alpine
    container_name: codejam-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

  # FastAPI Application
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: codejam-api
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql+asyncpg://codejam:${POSTGRES_PASSWORD:-codejam123}@postgres:5432/codejam
      REDIS_HOST: redis
      REDIS_PORT: 6379
      SECRET_KEY: ${SECRET_KEY}
      EXECUTION_TIMEOUT: 10
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./app:/app/app
      - ./alembic:/app/alembic
    command: >
      sh -c "
        alembic upgrade head &&
        uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
      "

  # RQ Worker (Code Execution)
  worker:
    build:
      context: .
      dockerfile: Dockerfile.worker
    container_name: codejam-worker
    environment:
      DATABASE_URL: postgresql+asyncpg://codejam:${POSTGRES_PASSWORD:-codejam123}@postgres:5432/codejam
      REDIS_HOST: redis
      REDIS_PORT: 6379
      SECRET_KEY: ${SECRET_KEY}
      EXECUTION_TIMEOUT: 10
    depends_on:
      redis:
        condition: service_healthy
    volumes:
      - ./app:/app/app
    # Security: Read-only filesystem, no network, memory limit
    read_only: true
    tmpfs:
      - /tmp
    mem_limit: 512m
    cpus: 1.0
    network_mode: "service:redis"  # Only access to Redis, no internet

volumes:
  postgres_data:
  redis_data:
